"""Safety primitives for future LLM-generated, read-only Cypher queries.

Submodules are intentionally not imported here so ontology schema export remains usable
without initializing the Neo4j driver layer.
"""
